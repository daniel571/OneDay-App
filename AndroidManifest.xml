<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.technion.nssl.oneday">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:allowBackup="true"
        android:name=".OneDay"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".facebook_login" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".MainActivity"
            android:label="@string/help_today_label"

            />
        <activity android:name=".UserActivity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".upcoming_vol"
            android:label="@string/upcoming_events"
            />
        <activity android:name=".waiting_vol"
            android:label="@string/waiting_list_vol"/>
        <activity android:name=".signed_vol"
            android:label="@string/signed_vol"/>
        <activity android:name=".user_profile"
            android:label="@string/user_settings"
            />
        <activity android:name=".donate_activity"
            android:label="@string/donate"/>
        <activity android:name=".contact_activity"
            android:label="@string/contact"></activity>
    </application>

</manifest>